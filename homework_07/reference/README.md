# Reference: legacy HW3 monolith

`legacy_baseline.cpp` is the original single-file baseline (HW3 port). It is **not compiled** by CMake and is kept only for historical comparison during reviews.

The supported implementation is under `src/` and is validated by `homework_07_baseline_tests` and the web simulator.
